hasta_seis <- 1:6
#factorial(5)

#hasta_seis %o% hasta_seis
#hasta_seis %*% hasta_seis
#round(mean(hasta_seis)) # promedio

#sample(hasta_seis, 2)
#sample(x = hasta_seis, size = 2)

# replace es con o sin repetición 
dice <- sample(hasta_seis, size = 2, replace = TRUE)
dice
# siempre va a ser lo mismo
#dice
#dice

#sum(dice)
#round(3.1415, digits = 2)
